export const OrderSuccess = (id) => (
        <div class="order__wrapper">
            <h2 class="order__title">Заказ оформлен</h2>
            <p class="order__id">Номер вашего заказа: {id} </p>
        </div>
    );
